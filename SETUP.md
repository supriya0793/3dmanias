# 3D Manias — AI Archviz Masterclass

## Run

```bash
cp .env.example .env
# add Razorpay Key ID + Key Secret into .env
npm install
npm run dev
```

Open http://localhost:5173

## Razorpay

Follow **RAZORPAY-SETUP.md** for dashboard keys, test payments, and going live.

## Course file

Keep `private/Ai-Archviz-Class.rar` private.
Put the Google Drive link in `.env` as `COURSE_FILE_LINK` — it is shown only after Razorpay payment is verified.
